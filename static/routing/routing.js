(function () {
  "use strict";
  var app = document.getElementById("routing-app");
  if (!app || typeof L === "undefined") return;
  var form = document.getElementById("routing-import-form"), status = document.getElementById("routing-import-status");
  var summary = document.getElementById("routing-summary"), create = document.getElementById("routing-create");
  var optimize = document.getElementById("routing-optimize"), target = document.getElementById("routing-target");
  var mode = document.getElementById("routing-mode"), results = document.getElementById("routing-results"), saved = document.getElementById("routing-saved-plans"), addPanel = document.getElementById("routing-add-stop"), available = document.getElementById("routing-available-stop"), addRoute = document.getElementById("routing-add-route"), addButton = document.getElementById("routing-add-stop-button");
  var map = L.map("routing-map").setView([48.42, -122.35], 11), layers = {}, currentImport = null, currentPlan = null;
  L.tileLayer("https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png", {maxZoom: 20, attribution: "© OpenStreetMap contributors © CARTO"}).addTo(map);
  function csrf() { var m = document.cookie.match(/csrftoken=([^;]+)/); return m ? decodeURIComponent(m[1]) : ""; }
  function clear() { Object.keys(layers).forEach(function (key) { map.removeLayer(layers[key]); }); layers = {}; results.innerHTML = ""; }
  function draw(data) {
    clear(); var bounds = [];
    data.routes.forEach(function (route, index) {
      var color = ["#d05a3a", "#287c72", "#6d54a3", "#bc7d2d", "#2b6ca3", "#a33d72"][index % 6], group = L.layerGroup();
      if (route.geometry && route.geometry.encoded_polylines) route.geometry.encoded_polylines.forEach(function (encoded) { var points = decode(encoded); if (points.length) L.polyline(points, {color: color, weight: 4, opacity: .75}).addTo(group); });
      else if (route.stops.length > 1) L.polyline(route.stops.map(function (stop) { return [stop.latitude, stop.longitude]; }), {color: color, weight: 3, opacity: .55, dashArray: "6 5"}).addTo(group);
      route.stops.forEach(function (stop) { L.circleMarker([stop.latitude, stop.longitude], {radius: 7, color: color, fillColor: color, fillOpacity: .9, weight: 2}).bindTooltip(route.route_number + "." + stop.sequence + " " + (stop.parcel_id || "stop")).addTo(group); bounds.push([stop.latitude, stop.longitude]); });
      group.addTo(map); layers[route.route_number] = group;
      var html = '<article class="routing-result" style="border-left-color:' + color + '"><header><strong>Route ' + route.route_number + '</strong> · ' + route.stop_count + ' stops <span><button type="button" class="routing-route-optimize" data-route-id="' + route.id + '">Optimize</button> <a href="/field/routes/plan/' + currentPlan + '/export/" download>CSV</a></span></header><ol>';
      route.stops.forEach(function (stop) { var options = data.routes.filter(function (candidate) { return candidate.id !== route.id; }).map(function (candidate) { return '<option value="' + candidate.id + '">Route ' + candidate.route_number + '</option>'; }).join(""); html += '<li>' + String(stop.parcel_id || "") + (stop.address ? " — " + String(stop.address) : "") + ' <button type="button" class="routing-lock-stop" data-stop-id="' + stop.id + '">' + (stop.manually_locked ? "Unlock" : "Lock") + '</button> <button type="button" class="routing-remove-stop" data-stop-id="' + stop.id + '">Remove</button> <select class="routing-move-target" data-stop-id="' + stop.id + '"><option value="">Move to…</option>' + options + '</select></li>'; });
      results.insertAdjacentHTML("beforeend", html + "</ol></article>");
    });
    if (bounds.length) map.fitBounds(bounds, {padding: [20, 20]});
  }
  function decode(encoded) {
    var points = [], index = 0, lat = 0, lon = 0;
    while (index < encoded.length) {
      var result = 0, shift = 0, byte;
      do { byte = encoded.charCodeAt(index++) - 63; result |= (byte & 31) << shift; shift += 5; } while (byte >= 32);
      lat += (result & 1) ? ~(result >> 1) : result >> 1; result = 0; shift = 0;
      do { byte = encoded.charCodeAt(index++) - 63; result |= (byte & 31) << shift; shift += 5; } while (byte >= 32);
      lon += (result & 1) ? ~(result >> 1) : result >> 1; points.push([lat / 1e6, lon / 1e6]);
    }
    return points;
  }
  function request(url, options) {
    options = options || {}; options.credentials = "same-origin"; options.headers = options.headers || {}; options.headers["X-CSRFToken"] = csrf();
    return fetch(url, options).then(function (response) { return response.text().then(function (text) { var data; try { data = JSON.parse(text); } catch (_) { throw Error("Server returned an HTML error page. Check the Railway logs."); } if (!response.ok) throw Error(data.error || "Request failed"); return data; }); });
  }
  function loadAvailable(data) { addPanel.hidden = false; addRoute.innerHTML = '<option value="">Select route…</option>' + data.routes.map(function (route) { return '<option value="' + route.id + '">Route ' + route.route_number + ' (' + route.stop_count + ')</option>'; }).join(""); request("/field/routes/plan/" + currentPlan + "/available-stops/").then(function (payload) { available.innerHTML = '<option value="">Select an unassigned stop…</option>' + payload.stops.map(function (stop) { return '<option value="' + stop.id + '">' + stop.parcel_id + " — " + stop.address + '</option>'; }).join(""); }).catch(function (error) { status.textContent = error.message; }); }
  form.addEventListener("submit", function (event) { event.preventDefault(); status.textContent = "Importing and validating…"; request(app.dataset.importUrl, {method: "POST", body: new FormData(form)}).then(function (data) { currentImport = data.import_id; create.disabled = false; summary.textContent = data.unique_stop_count + " unique stops from " + data.row_count + " source rows; " + data.summary.duplicate_rows + " duplicates, " + data.summary.missing_coordinates + " missing coordinates, " + data.summary.addressless_rows + " addressless rows."; status.textContent = "Import ready. Create clusters now; optimize later."; }).catch(function (error) { status.textContent = error.message; }); });
  saved.addEventListener("change", function () { if (!saved.value) return; status.textContent = "Loading saved plan…"; request("/field/routes/plan/" + saved.value + "/").then(function (data) { currentPlan = data.plan_id; currentImport = null; optimize.disabled = false; summary.textContent = data.route_count + " saved " + data.status + " " + data.mode + " routes."; draw(data); loadAvailable(data); status.textContent = "Saved plan loaded."; }).catch(function (error) { status.textContent = error.message; }); });
  create.addEventListener("click", function () { if (!currentImport) return; create.disabled = true; status.textContent = "Creating geographic clusters…"; request(app.dataset.planUrl, {method: "POST", headers: {"Content-Type": "application/json"}, body: JSON.stringify({import_id: currentImport, target_stop_count: Number(target.value), mode: mode.value})}).then(function (data) { currentPlan = data.plan_id; optimize.disabled = false; summary.textContent = data.route_count + " clusters created; " + data.summary.valid_stops + " stops assigned. Stop order is not optimized yet."; draw(data); loadAvailable(data); status.textContent = "Clusters saved. You can optimize this plan later."; create.disabled = false; }).catch(function (error) { status.textContent = error.message; create.disabled = false; }); });
  addButton.addEventListener("click", function () { if (!currentPlan || !available.value || !addRoute.value) return; addButton.disabled = true; request("/field/routes/plan/" + currentPlan + "/add-stop/", {method: "POST", headers: {"Content-Type": "application/json"}, body: JSON.stringify({import_row_id: Number(available.value), target_route: Number(addRoute.value)})}).then(function (data) { draw(data); loadAvailable(data); status.textContent = "Stop added and locked. Optimize the affected route when ready."; addButton.disabled = false; }).catch(function (error) { status.textContent = error.message; addButton.disabled = false; }); });
  optimize.addEventListener("click", function () { if (!currentPlan) return; optimize.disabled = true; status.textContent = "Optimizing saved stop order with the routing server…"; request("/field/routes/plan/" + currentPlan + "/optimize/", {method: "POST"}).then(function (data) { summary.textContent = data.route_count + " optimized " + data.mode + " routes; " + data.summary.valid_stops + " stops assigned."; draw(data); status.textContent = "Optimization complete."; optimize.disabled = false; }).catch(function (error) { status.textContent = error.message; optimize.disabled = false; }); });
  results.addEventListener("click", function (event) { var button = event.target.closest(".routing-route-optimize"); if (!button || !currentPlan) return; button.disabled = true; status.textContent = "Optimizing route…"; request("/field/routes/plan/" + currentPlan + "/route/" + button.dataset.routeId + "/optimize/", {method: "POST"}).then(function (data) { draw(data); status.textContent = "Route optimized."; }).catch(function (error) { status.textContent = error.message; button.disabled = false; }); });
  results.addEventListener("change", function (event) { var select = event.target.closest(".routing-move-target"); if (!select || !select.value || !currentPlan) return; status.textContent = "Moving stop and marking the plan for re-optimization…"; request("/field/routes/plan/" + currentPlan + "/move-stop/", {method: "POST", headers: {"Content-Type": "application/json"}, body: JSON.stringify({stop_id: Number(select.dataset.stopId), target_route: Number(select.value)})}).then(function (data) { draw(data); status.textContent = "Stop moved. Optimize the affected route when ready."; }).catch(function (error) { status.textContent = error.message; select.value = ""; }); });
  results.addEventListener("click", function (event) { var button = event.target.closest(".routing-lock-stop"); if (!button || !currentPlan) return; button.disabled = true; request("/field/routes/plan/" + currentPlan + "/stop/" + button.dataset.stopId + "/lock/", {method: "POST"}).then(function (data) { draw(data); status.textContent = "Stop lock updated."; }).catch(function (error) { status.textContent = error.message; button.disabled = false; }); });
  results.addEventListener("click", function (event) { var button = event.target.closest(".routing-remove-stop"); if (!button || !currentPlan || !window.confirm("Remove this stop from the route?")) return; button.disabled = true; request("/field/routes/plan/" + currentPlan + "/stop/" + button.dataset.stopId + "/remove/", {method: "POST"}).then(function (data) { draw(data); status.textContent = "Stop removed. Optimize the affected route when ready."; }).catch(function (error) { status.textContent = error.message; button.disabled = false; }); });
}());
