{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py sync_public_intelligence && python manage.py collectstatic --noinput && python -m uvicorn config.asgi:application --host 0.0.0.0 --port ${PORT:-8000} --proxy-headers",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
